Your investigation nailed the data flow, including how tax applies to the combined fee, not just the base. Time to modify.

Here is the scenario: your company operates in a jurisdiction where sales tax just increased from 13% to 17%. Update `calculate_delivery_fee` so the default `tax_rate` reflects this change.

Before you run your modified code, predict: what will `calculate_delivery_fee(120, 8)` return with the new rate? Give me your predicted value and confidence (1-5).
