Getting the right answer and understanding the mechanics are two different things. A correct prediction tells me you can compute the result; an explanation tells me you can debug it when something breaks in production.

You do not need to write anything formal. Just walk me through the data flow: what happens to `distance_km` and `base_fee` between the inputs and the `return` statement?